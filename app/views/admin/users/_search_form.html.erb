<%= form_with(local: true, method: :get, url: { action: "index" }, class: "my-3 flex gap-3") do |form| %>
  <%= form.text_field :query, value: params[:query].presence && params[:query], placeholder: 'Search users' %>
  <%= form.submit "Search", class: "btn btn-primary" %>
<% end %>
